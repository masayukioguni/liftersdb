.container style="margin-top: 10px" 
  

  
  div id="demodiv"

  table.table.table-striped.table-bordered id="example" cellpadding="0" cellspacing="0" border="0"
    thead
      tr
        th weight
        th SQ
        th BP
        th DL
        th total
        th total
        th date
    tbody
      - for record in @records do
        tr 
          td = record.weight
          td = record.squat
          td = record.benchpress
          td = record.deadlift
          td = record.total
          td = record.formula
          td = record.championship.date
  javascript:
    g = new Dygraph(
          document.getElementById("demodiv"),
          "/lifters/csv/#{params[:id]}?equipment=#{params[:equipment]}&type=#{params[:type]}",
          {
              labelsDiv: document.getElementById('status'),
              labelsSeparateLines: true,
              legend: 'always',
              colors: ["rgb(51,204,204)",
                       "rgb(255,100,100)",
                       "#00DD55",
                       "rgba(50,50,200,0.4)"],
              title:  'Record Graph',
              xlabel: 'Date',
              ylabel: 'Record',
              axisLineColor: 'white',
              drawXGrid: false
            }
        );
